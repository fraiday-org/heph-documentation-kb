# Setup

## Prerequisites

- Node.js 22+ for TypeScript and React repositories.
- Python 3.12+ for Python repositories.
- Terraform 1.14+ for infrastructure repositories.

## Environment

```bash
cp .env.example .env
```

## Verification

Use the repository-specific scripts in `package.json`, `pyproject.toml`, or shell scripts.
